@model IEnumerable<VidlyF.Web.Models.Movie>
@{
    ViewBag.Title = "Index";
}

<h2>Movies</h2>


<table id="movies" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Movie</th>
            <th>Genre</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var movie in Model)
            {
            <tr>
                <td>@Html.ActionLink(movie.Name, "Details", new { movie.Id }) </td>
                <td>@movie.Genre.Name</td>
            </tr>
        }
    </tbody>
</table>